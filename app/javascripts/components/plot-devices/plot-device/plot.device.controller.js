export default angular.module( 'campfire' ).controller(
  'PlotDeviceController',
  [
    '$scope', 'PlotDevice', '$routeParams',
    function( $scope, PlotDevice, $routeParams ) {
      // $scope.plotDevice = $scope.plotDevice || {}

    }
  ]
);
